{
  skills.skill-creator = {
    description = "Recipe for creating and extending agent and skill definitions.";
    content = ''
      # skill-creator

      When creating a new skill, ensure:

      - A stable filename that matches the skill key.
      - A clear one-line description.
      - A short prompt body with examples.
      - A test or acceptance note if applicable.
    '';
    resources = { };
    src = null;
  };
}
