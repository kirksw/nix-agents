description: "Test secrets-management skill"
prompts:
  - "I need to add an API key for Stripe to the project. How should I store it?"
providers:
  - id: anthropic:claude-sonnet-4-6
tests:
  - assert:
      - type: contains-any
        value:
          - "environment variable"
          - "sops"
          - "agenix"
      - type: not-contains
        value: "hardcode"
